FOUNDRY_PROJECT_ENDPOINT=https://<your-foundry-resource>.services.ai.azure.com/api/projects/<your-project-name>
# The backend derives https://<resource>.services.ai.azure.com/openai/v1 from the project endpoint for model calls.
# The published Prompt Agent and Hosted Agent use this project endpoint.
# Optional: enables trace retrieval for the Research Assistant prompt agent.
# The backend identity needs Log Analytics Reader on this Application Insights resource.
# FOUNDRY_APPLICATION_INSIGHTS_RESOURCE_ID=/subscriptions/<subscription-id>/resourceGroups/<resource-group>/providers/Microsoft.Insights/components/<app-insights-name>
# Optional override after usecases_agents/azure_architect_agent/hosted is deployed.
# The app defaults to hosted-assistant.
# FOUNDRY_HOSTED_AGENT_NAME=hosted-assistant
# Optional: register multiple hosted-agent implementations under one "Agents" use case with a
# variant dropdown. Comma-separated "key:Label:agent_name" triplets, one per deployed agent
# (see the framework folders under Foundry Agents/hosted_agent_*). When unset the app falls
# back to a single "default" variant built from FOUNDRY_HOSTED_AGENT_NAME above.
# FOUNDRY_HOSTED_AGENT_VARIANTS=azd:Hosted Agent (AZD):azure-architect-hosted-azd,code:Hosted Agent (Code):azure-architect-hosted-code,containers:Hosted Agent (Containers):azure-architect-hosted-container,langchain:Hosted Agent (LangChain):azure-architect-hosted-langchain,langgraph:Hosted Agent (LangGraph):azure-architect-hosted-langgraph,pydantic_ai:Hosted Agent (Pydantic AI):azure-architect-hosted-pydantic
# Optional override after usecases_agents/investment_planner_prompt is provisioned.
# The app defaults to investment-planner.
# FOUNDRY_INVESTMENT_PLANNER_AGENT_NAME=investment-planner
# Bundled phase-one retail assistant settings.
# The deployed retail entry agent is named "zava-shop-assistant-agent".
FOUNDRY_RETAIL_AGENT_NAME=zava-shop-assistant-agent
# Optional per-agent overrides; defaults match provision_retail_agents.py.
# FOUNDRY_RETAIL_INTERIOR_AGENT_NAME=zava-interior-designer-agent
# FOUNDRY_RETAIL_INVENTORY_AGENT_NAME=zava-inventory-agent
# FOUNDRY_RETAIL_LOYALTY_AGENT_NAME=zava-customer-loyalty-agent
# FOUNDRY_RETAIL_CART_AGENT_NAME=zava-cart-manager-agent
# FOUNDRY_RETAIL_HANDOFF_AGENT_NAME=zava-handoff-service-agent
# Set this only when intentionally using the bundled catalog without deployed Foundry agents.
FOUNDRY_RETAIL_OFFLINE_MODE=true
FOUNDRY_RETAIL_MODEL=gpt-4o-mini
# Provisioning-only settings for usecases_agents/investment_planner_prompt/agent (not read by the app).
# The provisioning script uses FOUNDRY_MODELS and derives the holdings URL from these existing
# storage settings. Set HOLDINGS_BLOB_URL only if the CSV is in a different container/path.
# HOLDINGS_BLOB_URL=https://<storage-account>.blob.core.windows.net/<container>/holdings.csv
# AZURE_STORAGE_ACCOUNT_URL=https://<storage-account>.blob.core.windows.net
# AZURE_STORAGE_CONTAINER_NAME=foundry-rag-documents
# Optional ARM ID overrides. If omitted, provisioning resolves both with Azure CLI.
# STORAGE_RESOURCE_ID=/subscriptions/<subscription-id>/resourceGroups/<resource-group>/providers/Microsoft.Storage/storageAccounts/<storage-account>
# PROJECT_RESOURCE_ID=/subscriptions/<subscription-id>/resourceGroups/<resource-group>/providers/Microsoft.CognitiveServices/accounts/<your-foundry-resource>/projects/<your-project-name>
# Optional FLUX provider endpoint override. Defaults to the resource origin from FOUNDRY_PROJECT_ENDPOINT.
# FOUNDRY_FLUX_ENDPOINT=https://<your-foundry-resource>.services.ai.azure.com
# Optional bootstrap list. The app stores model deployment names in the configured persistence backend.
FOUNDRY_MODELS=gpt-5.5,gpt-4o-mini

# Optional named Foundry bindings. Suffixes are discovered dynamically and the database
# maps each use case to one suffix; adding REGION3 requires no code change.
REGION1=swedencentral
FOUNDRY_PROJECT_ENDPOINT_REGION1=https://<region-1-resource>.services.ai.azure.com/api/projects/<project-name>
#=gpt-5.5,gpt-5.6-sol

REGION2=eastus
FOUNDRY_PROJECT_ENDPOINT_REGION2=https://<region-2-resource>.services.ai.azure.com/api/projects/<project-name>
#FOUNDRY_MODELS_REGION2=gpt-5.5,gpt-4o-mini
# Optional when Speech uses a different resource name than the Foundry endpoint.
# AZURE_SPEECH_ENDPOINT_REGION2=https://<speech-resource>.cognitiveservices.azure.com

# Local development defaults to SQLite. Azure deployments explicitly use Cosmos DB.
PERSISTENCE_BACKEND=sqlite
# SQLITE_DATABASE_PATH=data/foundry_chat.sqlite3

# Required when PERSISTENCE_BACKEND=cosmos. This is a base name; the app uses the -v3 container.
AZURE_COSMOS_ENDPOINT=https://<your-cosmos-account>.documents.azure.com:443/
AZURE_COSMOS_DATABASE_NAME=<shared-database-name>
AZURE_COSMOS_CONTAINER_NAME=foundry-chat-app
# Set true only when the identity/key may create containers; otherwise provision it first with /partition_key.
# AZURE_COSMOS_CREATE_CONTAINER=false
# Optional for local development. When omitted, DefaultAzureCredential is used.
# AZURE_COSMOS_KEY=<cosmos-account-key>

# Optional: enables the Foundry Realtime voice demo.
# Defaults to FOUNDRY_PROJECT_ENDPOINT when omitted. You can also set the
# direct OpenAI-compatible endpoint shown by Foundry.
# FOUNDRY_REALTIME_ENDPOINT=https://<your-foundry-resource>.services.ai.azure.com/openai/v1
FOUNDRY_REALTIME_MODEL=gpt-realtime-2.1
# Optional fallback when deployment discovery is unavailable. The UI normally
# gets realtime transcription deployment choices from /api/models.
# FOUNDRY_REALTIME_TRANSCRIPTION_MODEL=gpt-realtime-whisper
# FOUNDRY_REALTIME_TRANSCRIPTION_MODELS=gpt-realtime-whisper,gpt-live-transcribe
FOUNDRY_REALTIME_TRANSLATION_MODEL=gpt-realtime-translate

# Optional: enables Azure Translator - Text Translation.
# Defaults to the Cognitive Services endpoint derived from FOUNDRY_PROJECT_ENDPOINT.
# FOUNDRY_TRANSLATOR_ENDPOINT=https://<your-foundry-resource>.cognitiveservices.azure.com/
# Optional API-key fallback. Omit to use Microsoft Entra ID through AZURE_CREDENTIAL_TYPE.
# FOUNDRY_TRANSLATOR_KEY=<your-translator-key>
# Optional Azure Language API version for language detection, PII redaction, and health analysis.
# FOUNDRY_LANGUAGE_API_VERSION=2026-05-01

# Optional: enables Content Extractor with Azure Content Understanding.
# Defaults to the Cognitive Services endpoint derived from FOUNDRY_PROJECT_ENDPOINT.
# FOUNDRY_CONTENT_UNDERSTANDING_ENDPOINT=https://<your-foundry-resource>.cognitiveservices.azure.com/
# Optional API-key fallback. Omit to use Microsoft Entra ID through AZURE_CREDENTIAL_TYPE.
# FOUNDRY_CONTENT_UNDERSTANDING_KEY=<your-content-understanding-key>

# Optional static use-case to /api/models bucket mapping. Omit to use defaults.
# USE_CASE_MODEL_MAP={"text_chat":"text_models","youtube_summary":{"text":"text_models","transcription":"transcription_models"},"youtube_realtime_transcription":"realtime_transcription_models"}
# Realtime transcription exposes per-session language, delay, and VAD controls
# in the UI. The WebSocket flavor defaults to app-side silence commits.

# Optional: enables the Document Q&A RAG use case.
# Uses Azure Blob Storage for original files, Azure AI Search for retrieval, and Foundry for embeddings/chat.
AZURE_STORAGE_ACCOUNT_URL=https://<your-storage-account>.blob.core.windows.net
AZURE_STORAGE_CONTAINER_NAME=foundry-rag-documents
# Optional prefix-backed gallery for image-to-image starter images. Seed with scripts/seed_image_samples.py.
# Content Extractor samples use the same private container. Seed local files with:
# python scripts/seed_content_extractor_samples.py document ./samples/documents --account-url "$AZURE_STORAGE_ACCOUNT_URL"
# python scripts/seed_content_extractor_samples.py audio ./samples/audio --account-url "$AZURE_STORAGE_ACCOUNT_URL"
# Files are stored under document-samples/ and audio-samples/.
AZURE_SEARCH_ENDPOINT=https://<your-search-service>.search.windows.net
AZURE_SEARCH_INDEX_NAME=foundry-document-rag
FOUNDRY_EMBEDDING_MODEL=text-embedding-3-small
# Optional if the index already exists. Defaults to the dimensions returned by Foundry on upload.
# FOUNDRY_EMBEDDING_DIMENSIONS=1536

# Optional: enables the traditional Foundry STT -> chat -> TTS voice pipeline demo.
# Deployment names must match audio models deployed in your Foundry project.
FOUNDRY_TRANSCRIPTION_MODEL=gpt-4o-mini-transcribe
FOUNDRY_TTS_MODEL=gpt-audio-mini
FOUNDRY_TTS_VOICE=alloy

# Optional: enables standalone transcription.
# The model is selected by the Speech service backing this Foundry resource.
AZURE_SPEECH_ENDPOINT=https://<your-foundry-resource>.cognitiveservices.azure.com/
# Optional fallback when local/API-key authentication is enabled. Microsoft Entra ID is preferred.
# AZURE_SPEECH_KEY=<your-speech-resource-key>
AZURE_SPEECH_TRANSCRIPTION_MODEL=MAI-Transcribe-1.5

# Optional: enables the managed Voice Live travel concierge use case.
# Use the resource root, not a project path. Entra ID authentication is used by default.
AZURE_VOICELIVE_ENDPOINT=https://<your-foundry-resource>.services.ai.azure.com/
AZURE_VOICELIVE_MODEL=gpt-realtime
AZURE_VOICELIVE_VOICE=en-US-Ava:DragonHDLatestNeural

# Optional: used by the Model metrics dashboard for estimated cost.
FOUNDRY_INPUT_TOKEN_COST_PER_1K=0
FOUNDRY_OUTPUT_TOKEN_COST_PER_1K=0

# Optional: enables automatic model deployment discovery and the Admin > Deploy model UI.
FOUNDRY_SUBSCRIPTION_ID=<your-subscription-id>
FOUNDRY_RESOURCE_GROUP=<your-resource-group>
FOUNDRY_ACCOUNT_NAME=<your-foundry-resource-name>

# Local authentication toggle. Use disabled to bypass Microsoft sign-in while
# developing, even if ENTRA_LOCAL_* values remain in this file. Use local to test
# the signed-cookie development flow, or container_apps behind Azure Container Apps auth.
APP_AUTH_MODE=disabled

# Optional: Microsoft Entra sign-in during local development. Register a confidential web app and
# add the exact redirect URI below to Authentication > Web > Redirect URIs, then set
# APP_AUTH_MODE=local.
# ENTRA_LOCAL_CLIENT_ID=<app-registration-client-id>
# ENTRA_LOCAL_CLIENT_SECRET=<app-registration-client-secret-value>
# ENTRA_LOCAL_TENANT_ID=<tenant-id>
# ENTRA_LOCAL_REDIRECT_URI=http://localhost:5173/api/auth/callback
# ENTRA_LOCAL_SESSION_SECRET=<long-random-value-at-least-32-characters>
# Container Apps mode also requires the Entra tenant used by the authentication proxy.
# APP_AUTH_TENANT_ID=<tenant-id>
# Administrators allowed to change global model settings, register models, and create
# Foundry deployments. Comma-separated Entra object IDs or normalized emails; matching
# is case-insensitive. Use immutable object IDs in production; email is intended mainly
# for local ergonomics. An empty list denies every caller when authentication is enabled.
# ADMIN_PRINCIPALS=00000000-0000-0000-0000-000000000000,admin@example.com
# Optional comma-separated WebSocket origins. Same-origin is required when omitted.
# ALLOWED_ORIGINS=http://localhost:5173
# Maximum concurrent Foundry model operations per application process.
# MODEL_CALL_CONCURRENCY=8
# Application log threshold: DEBUG, INFO, WARNING, ERROR, or CRITICAL.
# LOG_LEVEL=INFO
